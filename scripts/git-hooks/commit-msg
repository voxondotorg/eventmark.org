#!/bin/sh
# Strip Cursor / agent co-author trailers before the commit is created.
COMMIT_MSG_FILE="$1"
TMP="${COMMIT_MSG_FILE}.strip"
grep -viE '^co-authored-by:.*(cursor|cursoragent@)' "$COMMIT_MSG_FILE" > "$TMP" || true
mv "$TMP" "$COMMIT_MSG_FILE"
exit 0
